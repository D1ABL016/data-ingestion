# Alembic — run from repository root (see README).
# Real URL comes from DATABASE_URL via migrations/env.py → data_ingestion.config.settings

[alembic]
script_location = migrations
prepend_sys_path = .
version_path_separator = os
sqlalchemy.url = driver://user:pass@localhost/dbname

[post_write_hooks]
